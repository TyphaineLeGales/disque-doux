import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export const Timeline = () => {
  return (
    <SafeAreaView className="flex-1 items-center justify-center bg-slate-600">
      <Text>Menu timeline</Text>
    </SafeAreaView>
  );
};
