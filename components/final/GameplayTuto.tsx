import React from 'react';
import { View } from 'react-native';
type GameplayTutoProps = {
  onDone: Function;
  id: string;
  debug?: boolean;
};
export default function FindTools(props: GameplayTutoProps) {
  // TODO : should return the right rive asset given a level index
  return <View />;
}
